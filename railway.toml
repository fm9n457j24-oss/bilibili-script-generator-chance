[deploy]
# Railway 部署配置
# Railway 会自动设置 PORT 环境变量，应用需要监听该端口

[build]
# 使用 Dockerfile 构建
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy.healthcheck]
path = "/health"
timeout = 10
interval = 30
